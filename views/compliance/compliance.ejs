<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>VITA - Compliance Management</title>

    <script src="https://unpkg.com/htmx.org@1.9.12"></script>

    <style>
        :root {
            --primary: #2563eb;
            --primary-dark: #1d4ed8;
            --bg: #ffffff;
            --fg: #0f172a;
            --muted: #64748b;
            --border: #e2e8f0;
            --card: #ffffff;
            --secondary: #f8fafc;
            --radius: 0.5rem;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: system-ui, sans-serif;
            background: var(--bg);
            color: var(--fg);
        }

        a {
            color: inherit;
            text-decoration: none;
        }

        .container {
            max-width: 1280px;
            margin: auto;
            padding: 1rem;
        }

        header {
            position: sticky;
            top: 0;
            background: rgba(255, 255, 255, 0.95);
            border-bottom: 1px solid var(--border);
            z-index: 50;
        }

        .header-inner {
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 64px;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 1.5rem;
            font-weight: 800;
        }

        .logo img {
            height: 40px;
        }

        nav {
            display: flex;
            gap: 2rem;
        }

        nav a:hover {
            color: var(--primary);
        }

        h1 {
            font-size: 2.3rem;
            font-weight: 900;
            margin: 1rem 0 0.5rem;
        }

        h1 span {
            color: var(--primary);
        }

        p {
            color: var(--muted);
            margin-bottom: 1.5rem;
        }

        /* TABLE */
        .table-wrapper {
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background: var(--card);
            border-radius: var(--radius);
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
        }

        th,
        td {
            padding: 0.75rem 1rem;
            border-bottom: 1px solid var(--border);
            text-align: left;
            font-size: 0.85rem;
            vertical-align: top;
        }

        th {
            background: var(--secondary);
            font-weight: 700;
            text-transform: uppercase;
            font-size: 0.7rem;
            letter-spacing: 0.05em;
        }

        tr:hover {
            background: #f8fafc;
        }

        /* STATUS */
        .status-pill {
            padding: 0.25rem 0.6rem;
            border-radius: 999px;
            font-size: 0.7rem;
            font-weight: 700;
            display: inline-block;
        }

        .valid {
            background: #dcfce7;
            color: #166534;
        }

        .expiring {
            background: #fef3c7;
            color: #92400e;
        }

        .expired {
            background: #fee2e2;
            color: #991b1b;
        }

        /* FORM */
        select,
        input[type="date"] {
            padding: 0.4rem 0.6rem;
            border: 1px solid var(--border);
            border-radius: var(--radius);
            font-size: 0.8rem;
            min-width: 110px;
        }

        td form {
            display: flex;
            flex-direction: column;
            gap: 0.4rem;
        }

        .btn {
            padding: 0.4rem 0.9rem;
            font-size: 0.75rem;
            border-radius: var(--radius);
            border: 1px solid var(--border);
            background: #f1f5f9;
            font-weight: 600;
            cursor: pointer;
            transition: 0.2s;
        }

        .btn:hover {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
        }

        .upload-form input[type="file"] {
            font-size: 0.75rem;
            max-width: 180px;
        }

        /* SUPPLIER BADGE */
        .trusted-supplier {
            background: #eff6ff;
            border: 1px solid #bfdbfe;
            padding: 0.25rem 0.5rem;
            border-radius: var(--radius);
        }

        .trusted-badge {
            margin-left: 0.5rem;
            padding: 0.15rem 0.45rem;
            font-size: 0.6rem;
            border-radius: 999px;
            background: linear-gradient(135deg, #2563eb, #1d4ed8);
            color: white;
            font-weight: 700;
        }

        .muted {
            color: var(--muted);
        }

        /* Upload column */
        .upload-cell {
            display: flex;
            flex-direction: column;
            gap: 0.35rem;
        }

        .upload-cell input[type="file"] {
            font-size: 0.7rem;
            max-width: 160px;
        }

        .upload-cell button {
            align-self: flex-start;
        }

        /* Action (Save) */
        .action-cell {
            display: flex;
            align-items: center;
        }
    </style>
</head>

<body>

    <header>
        <div class="container header-inner">
            <div class="logo">
                <img src="/assets/vita-logo.png">
                <span>VITA</span>
            </div>
            <nav>
                <a href="/">Home</a>
                <a href="/about-us">About</a>
                <a href="/subscription">Subscription</a>
                <a href="/career">Career</a>
                <a href="/contact">Contact</a>
            </nav>
            <div>
                <% if (userName) { %>
                    Welcome <%= userName %>
                        <% } else { %>
                            <a href="/register" class="btn">Sign Up</a>
                            <% } %>
            </div>
        </div>
    </header>

    <div class="container">

        <h1>Compliance <span>Management</span></h1>
        <p>Automated tracking of insurance, licensing, and safety compliance.</p>

        <div class="table-wrapper">
            <table>
                <thead>
                    <tr>
                        <th>Supplier</th>
                        <th>Type</th>
                        <th>Document</th>
                        <th>Status</th>
                        <th>Expiry</th>
                        <% if (role==='supplier' ) { %>
                            <th>Action</th>
                            <th>Upload</th>
                            <% } %>
                    </tr>
                </thead>

                <tbody>
                    <% records.forEach(r=> { %>
                        <tr>

                            <td>
                                <strong class="<%= r.supplier_subscription === 'pro' ? 'trusted-supplier' : '' %>">
                                    <%= r.supplier_name %>
                                </strong>
                                <% if (r.supplier_subscription==='pro' ) { %>
                                    <span class="trusted-badge">Trusted</span>
                                    <% } %>
                            </td>

                            <td>
                                <%= r.compliance_type %>
                            </td>

                            <td>
                                <% if (r.document_path) { %>
                                    <a href="/<%= r.document_path %>" target="_blank">
                                        <%= r.document_name %>
                                    </a>
                                    <% } else { %>
                                        <span class="muted">No document</span>
                                        <% } %>
                            </td>

                            <% if (role==='supplier' ) { %>
                                <!-- Status cell: input belongs to the update form via form="update-<%= r.id %>" -->
                                <td>
                                    <select name="status" form="update-<%= r.id %>">
                                        <option value="Valid" <%=r.status==='Valid' ? 'selected' : '' %>>Valid</option>
                                        <option value="Expiring" <%=r.status==='Expiring' ? 'selected' : '' %>>Expiring
                                        </option>
                                        <option value="Expired" <%=r.status==='Expired' ? 'selected' : '' %>>Expired
                                        </option>
                                    </select>
                                </td>

                                <!-- Expiry cell: date input also belongs to the update form -->
                                <td>
                                    <input type="date" name="expiry_date" value="<%= r.expiry_date %>"
                                        form="update-<%= r.id %>">
                                </td>

                                <!-- ACTION: chỉ Save -->
                                <td>
                                    <form id="update-<%= r.id %>" action="/compliance/update/<%= r.id %>" method="POST"
                                        class="action-cell">
                                        <button class="btn" type="submit">Save</button>
                                    </form>
                                </td>

                                <!-- UPLOAD: cột riêng -->
                                <td>
                                    <form action="/compliance/upload/<%= r.id %>" method="POST"
                                        enctype="multipart/form-data" class="upload-cell">
                                        <input type="file" name="document" required>
                                        <button class="btn" type="submit">Upload</button>
                                    </form>
                                </td>
                                <% } else { %>
                                    <td>
                                        <span class="status-pill <%= r.status.toLowerCase() %>">
                                            <%= r.status %>
                                        </span>
                                    </td>
                                    <td>
                                        <%= new Date(r.expiry_date).toLocaleDateString() %>
                                    </td>
                                    <% } %>

                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>
    </div>

</body>

</html>